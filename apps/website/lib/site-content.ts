/**
 * Öffentliche Profildaten — übernommen aus `apps/website/lib/site.ts`.
 * Bei Änderungen dort manuell synchron halten (kein Cross-Import).
 */
export const sitePublicUrl = "https://resqbrain.de" as const;

export const contactEmail = "triggerhub@outlook.com" as const;

export const repositoryUrl = "https://github.com/Alox040/ResQBrain" as const;
