export type SiteMetadata = {
  title: string;
  description: string;
  locale: string;
};

export type FaqItem = {
  question: string;
  answer: string;
};

export type LinkItem = {
  label: string;
  href: string;
};
