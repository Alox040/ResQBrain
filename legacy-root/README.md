# Legacy root archive

This folder contains archived root-level code that is not part of the active website runtime.

- Treat all files here as legacy reference only.
- Do not use these files as runtime content sources.
- Active website runtime code is under `apps/website/`.
