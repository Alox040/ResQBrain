/** Blueprint lookup medication — implementation lives in `packages/domain/models`. */
export type { Medication } from '../../../models/Medication';
export { createMedication } from '../../../models/Medication';
